<%- include("partials/header.ejs") %>

<header> 
    <nav class="navbar">
        <div class="container-md border-bottom py-3 mb-4">
            <a href="/" class="logo erase-link-underscore"><span>thoughts.</span></a>
            <nav class="nav gap-3">
                <form action="/">
                    <input type="submit" class="btn btn-secondary poppins-regular" value="discard" />
                </form>
                <button type="submit" class="btn btn-success poppins-regular" form="newPost">save</button>
            </nav>
        </div>
    </nav>
</header>

<div class="container-md">
    <form action="/edit/<%= id %>/<%= title %>" method="post" id="newPost">
        <div class="mb-3">
            <textarea type="text" class="border-0 shadow-none w-100 no-resize-textarea title" id="title txtarea" name="title" placeholder="Title" required><%= title %></textarea>
        </div>
        <div class="mb-3">
            <textarea type="text" class="border-0 shadow-none w-100 no-resize-textarea content" id="content" name="content" placeholder="Express your thoughts..." required><%= content %></textarea>
        </div>
    </form>
</div>

</main>
</body>
</html>